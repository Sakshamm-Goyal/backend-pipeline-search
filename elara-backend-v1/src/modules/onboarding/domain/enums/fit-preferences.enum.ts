export enum TopFit {
  TIGHT = 'tight',
  FITTED = 'fitted',
  RELAXED = 'relaxed',
  OVERSIZED = 'oversized',
}

export enum BottomFit {
  TIGHT = 'tight',
  FITTED = 'fitted',
  RELAXED = 'relaxed',
  LOOSE = 'loose',
}

export enum WaistPreference {
  LOW_RISE = 'low-rise',
  MID_RISE = 'mid-rise',
  HIGH_RISE = 'high-rise',
}

export enum SleeveFit {
  TIGHT = 'tight',
  FITTED = 'fitted',
  RELAXED = 'relaxed',
  OVERSIZED = 'oversized',
}

export enum TopLength {
  CROPPED = 'cropped',
  REGULAR = 'regular',
  TUNIC = 'tunic',
  LONG = 'long',
}

export enum BottomLength {
  SHORT = 'short',
  MIDI = 'midi',
  REGULAR = 'regular',
  LONG = 'long',
  ANKLE = 'ankle',
}
